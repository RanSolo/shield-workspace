All required evidence is supplied here. Do not inspect files, request tools, emit XML, or mention unavailable paths. Mission issue-174 at d5bde71. Existing private passcodeFromOptions has a TTY branch that resumes process.stdin and duplicates cleanup on success, Ctrl-C, and empty Enter; cleanup omits pause. Non-TTY --passcode-stdin async iteration must not change. Tests can directly import an internal module export from dist/mission-cli.mjs but no package export may be added. Correct the blueprint by returning exactly four one-paragraph headings: CORRECTED_FUNCTION_SHAPE: name a small exported-for-internal-test readInteractivePasscode(input, output) helper using structural fakeable stream interfaces, listener-before-resume, one idempotent cleanup that removes listener, restores raw mode, and pauses after every terminal outcome and synchronous stream failure. CORRECTED_AC3_TEST: retain end-to-end authorize test and assert the persisted mission journal contains exactly one governance authorization entry after successful exit. NON_LEAKAGE_ASSERTION: captured stdout/stderr must not contain the dummy passcode. PACKET_VERDICT: say whether this evidence is sufficient. No code block and no implementation claim.

