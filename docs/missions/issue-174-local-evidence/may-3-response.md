CORRECTED_FUNCTION_SHAPE: Extract a small exported-for-internal-test helper readInteractivePasscode(input, output) using structural fakeable stream interfaces where input/output are any objects exposing readable or writable semantics suitable for testing, register the listener before resuming process.stdin, and provide one idempotent cleanup that removes the listener, restores raw mode, pauses after every terminal outcome including synchronous stream failures, and is invoked exactly once per call regardless of success, Ctrl-C, empty Enter, or error.

CORRECTED_AC3_TEST: Retain the existing end-to-end authorize test against passcodeFromOptions and assert the persisted mission journal contains exactly one governance authorization entry after successful exit, confirming that the corrected interactive path still produces a single authoritative authorization record without duplication or omission.

NON_LEAKAGE_ASSERTION: Captured stdout/stderr from the interactive passcode flow must not contain the dummy passcode value supplied during testing, ensuring no secret material leaks through logging, echo, or error formatting in either the TTY success, Ctrl-C cancellation, empty Enter retry, or synchronous failure paths.

PACKET_VERDICT: Insufficient; the blueprint omits explicit handling requirements for synchronous stream failures within readInteractivePasscode and does not specify whether non-TTY --passcode-stdin callers must be verified unchanged post-refactor, nor does it confirm that the single governance authorization assertion covers both TTY and non-TTY code paths to prevent regression of the existing async iteration contract.
