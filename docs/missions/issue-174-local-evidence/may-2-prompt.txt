Mission issue-174, base d5bde71, blueprint correction only. Your prior AC3 assertion was invalid: passcode text must never appear in stdout, and counting it cannot prove authorization appended exactly once. Also passcodeFromOptions uses internal ParsedOptions, so merely adding optional stream parameters does not establish a clean focused test seam. Return exactly CORRECTED_FUNCTION_SHAPE, CORRECTED_AC3_TEST, NON_LEAKAGE_ASSERTION, and PACKET_VERDICT. Prefer a small internal exported interactive helper with structural stream interfaces if justified; keep --passcode-stdin unchanged. AC3 must verify the durable journal has exactly one authorization entry after successful command completion. No implementation or authority claim.

