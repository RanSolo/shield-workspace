# May blueprint — issue #196 governed local Mack

Implement only the Fury-approved plan in
`docs/missions/issue-196-local-mack-plan.md`.

Create one read-only host composition that combines an exact replay-bound
validation request, complete repository observations, exact-registry
host-executed validation lanes, pre/post-probed local LM Studio runtime
identity, a narrow model-analysis candidate, and the existing
`mack.validation.v0` evaluator into immutable non-authoritative evidence.

The model must not emit the v0 host report, choose commands, claim host results,
receive repository or process tools, write files, or supply identity/status.
The host constructs the final v0 report from the frozen request, exact command
definitions, real receipts, and model findings. All PASS eligibility is
host-derived. Synthetic test evidence is always ineligible. Preserve existing
hosted Mack, v0 reports, generic V0.3 routing, and Daisy/May local paths.

Derive repository diff/source context from the bound Git objects; caller-frozen
digests alone are insufficient. Require non-vacuous required-scenario mappings:
each required scenario maps to at least one required lane, and every mapping is
non-empty, unique, and closed over frozen scenario IDs.

Edit exactly the ten writable paths frozen in the plan. If another path is
required, stop for Fury. Do not touch #149, invoke its Gemma/Mack validation,
merge, deploy, release, run #137, or enter #29.
