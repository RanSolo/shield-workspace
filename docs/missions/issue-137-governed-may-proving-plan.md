# Issue #137 governed May proving blocker disposition

## Frozen objective

Attempt one exact-bound governed local-Gemma May dispatch for issue #137 and
stop at its post-implementation review gate without performing the external
fixture run or entering issue #29.

## Exact finding

The signed operator flow produced a schema-2 supervised journal, while the
canonical dispatch accepts only schema-9 profile-aware journals with distinct
signed Wheels Up authority and a signed active May runtime binding. No shipped
CLI or supported host command composes those profile-aware records. The
canonical bridge therefore fails closed before May invocation.

## Plan for this mission

1. Preserve the exact signed mission and local-Daisy evidence.
2. Obtain Fury's architecture disposition on whether the missing supported
   profile-aware initiation/signing route is a true prerequisite.
3. If Fury confirms, stop this mission before May and record the blocker on
   issue #137. Do not create a misleading implementation commit or proving PR.
4. Freeze a separate child issue for the smallest supported schema-9 operator
   path. That child must preserve the existing authority contract, require
   exact Coulson signatures, and add no verbal or caller-asserted authority.
5. Resume this exact proving objective only after the prerequisite is merged,
   then create a fresh signed schema-9 mission and runtime binding for Gemma.

## Explicit exclusions

- no schema-plane translation or reuse of the schema-2 signature;
- no fabricated implementation authority or runtime binding;
- no local May/model invocation;
- no external fixture execution or issue #29 work;
- no merge, deployment, release, or human acceptance claim.

## Validation

- inspect the exact journal schema and replay result;
- inspect the canonical dispatch's `profile-aware` check;
- inspect the CLI's accepted brief and journal producers;
- run the focused local-Daisy review and preserve only its final handoff and
  host-observed statistics;
- require Fury to review this exact plan revision before external publication.

